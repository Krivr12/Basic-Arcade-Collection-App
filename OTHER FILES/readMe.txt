So in order to run the app.py file. First you need to install all the necessary requirements. The steps are:
1. First download the entire DSA APP file
2. Open you terminal then type 
    pip install -r requirements.txt
3. After that, afaik you can now just run the 'app.py' file with no problem
